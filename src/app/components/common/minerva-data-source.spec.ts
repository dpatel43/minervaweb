import { MinervaDataSource } from './minerva-data-source';

describe('MinervaDataSource', () => {
  it('should create an instance', () => {
    expect(new MinervaDataSource()).toBeTruthy();
  });
});
